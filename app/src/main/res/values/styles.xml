<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Custom application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:background">@color/activity_background_color</item>

        <item name="windowActionBar">false</item>
        <item name="background">@color/activity_background_color</item>
    </style>

    <style name="actionBarStyle" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="android:background">@color/action_bar_bg_color</item>

        <!-- support library compatibility -->
        <item name="background">@color/action_bar_bg_color</item>
    </style>

</resources>
